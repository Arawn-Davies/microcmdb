@model IEnumerable<uCMDB.API.Models.Service_Host>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Host_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Host_IP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Host_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Host_IP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Host_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Host_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Host_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
