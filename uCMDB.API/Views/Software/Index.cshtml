@model IEnumerable<uCMDB.API.Models.Software>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Developer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LicenceType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InstallExecPath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Developer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LicenceType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InstallExecPath)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Software_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Software_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Software_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
